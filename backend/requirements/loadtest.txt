# Only needed to run Milestone 5E load tests -- not a runtime dependency
# of the application itself. Install with: pip install -r requirements/loadtest.txt
-r base.txt
gunicorn>=22.0,<23.0
httpx>=0.27,<1.0
